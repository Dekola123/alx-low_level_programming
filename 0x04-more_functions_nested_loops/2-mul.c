#include "main.h"
/**
 * int mul(int a, int b) - function to multiply two integers
 * @a: first integer
 * @b: second integer
 *
 * Return - Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
